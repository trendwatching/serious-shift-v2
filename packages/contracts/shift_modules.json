{
  "$comment": [
    "Canonical module types for the shift / sub-shift reading views.",
    "A shift page is an ordered list of {type, data} modules; this file is the",
    "contract between the pipeline (which emits them) and the front end (which",
    "renders them). Adding a type means: add it here, emit it from the pipeline's",
    "MODULE_TEMPLATE, and register a component in",
    "apps/frontend/src/shift/modules.jsx. Unknown types fail publication and the",
    "front end also surfaces a defensive unavailable-section fallback.",
    "Guarded by apps/pipeline/tests/test_shift_modules_contract.py.",
    "One type breaks that pattern: `innovations` is not generated at all. The",
    "backend hydrates it into each shift row when it builds a route fragment, so",
    "an innovation pushed by the upstream database appears within the response",
    "cache TTL instead of waiting for the next weekly publication. The order",
    "below is still what decides where it lands \u2014 apps/backend/src/innovations.rs",
    "mirrors these two lists as consts and a test fails if they drift."
  ],
  "version": 5,
  "$order_comment": [
    "Canonical reading order.",
    "The export sorts every module list by this, so changing the order here",
    "re-composes each page on the next --export-only with no regeneration, and",
    "the front end sorts by it again at render so an already-published document",
    "reads correctly too. Types absent from a page are simply skipped.",
    "The sub-shift list sits directly after the peel tabs, which is where the",
    "delivered design build puts it. An earlier Miro content mockup put it at the",
    "bottom of the page and this file followed that; the design supersedes it."
  ],
  "order": {
    "key_trend": [
      "dek",
      "from_to",
      "pull_quote",
      "stat_band",
      "peel_tabs",
      "sub_shift_list",
      "human_needs",
      "tension_band",
      "timeline",
      "industries",
      "territories",
      "innovations",
      "voices",
      "related_shifts",
      "rich_text"
    ],
    "sub_trend": [
      "lede",
      "from_to_solid",
      "tension_band",
      "stat_band",
      "peel_tabs",
      "human_needs",
      "signals",
      "counter_signals",
      "evidence",
      "timeline",
      "territories",
      "innovations",
      "rich_text"
    ]
  },
  "$visibility_comment": [
    "Which of the generated modules a page actually shows, by scope and sphere.",
    "",
    "Presentation only. The publication always carries every module it generated",
    "and validate_map() still requires them, so hiding a section can never make a",
    "run unpublishable and un-hiding one never needs a republication. The filter is",
    "applied by the backend when it builds a route fragment, which is what lets an",
    "editor flip a row in shift_module_visibility and see the change within",
    "DOC_CACHE_TTL instead of at the next weekly run.",
    "",
    "This block is the DEFAULT, taken from the delivered design build. A row in",
    "shift_module_visibility overrides it for one (scope, domain_id, module_type);",
    "domain_id '*' overrides it for every sphere at once. A page composed from a",
    "shift_module_overrides row skips the filter entirely \u2014 an editor who authored",
    "the whole module list has already decided what appears.",
    "apps/backend/src/module_policy.rs mirrors this as a const and",
    "default_visibility_matches_the_contract fails if the two drift.",
    "",
    "Two groups are hidden here. The first is per-sphere: the design gates",
    "industries and territories to Consumers on a key shift, and human_needs and",
    "territories to Consumers on a sub-shift. The second is everywhere: pull_quote,",
    "tension_band, voices and related_shifts on key shifts, and signals,",
    "counter_signals and evidence on sub-shifts, are all still generated but the",
    "design build renders none of them. They are kept rather than cut so that",
    "turning one on later is one row and no model spend.",
    "",
    "domain_id is the database id, so Organisations is spelled with an s here and",
    "only here. Everything a reader sees is US spelling."
  ],
  "visibility": {
    "hidden": {
      "key_trend": {
        "society": [
          "pull_quote",
          "tension_band",
          "voices",
          "related_shifts",
          "industries",
          "territories"
        ],
        "economy": [
          "pull_quote",
          "tension_band",
          "voices",
          "related_shifts",
          "industries",
          "territories"
        ],
        "organisations": [
          "pull_quote",
          "tension_band",
          "voices",
          "related_shifts",
          "industries",
          "territories"
        ],
        "consumers": [
          "pull_quote",
          "tension_band",
          "voices",
          "related_shifts"
        ]
      },
      "sub_trend": {
        "society": [
          "signals",
          "counter_signals",
          "evidence",
          "human_needs",
          "territories"
        ],
        "economy": [
          "signals",
          "counter_signals",
          "evidence",
          "human_needs",
          "territories"
        ],
        "organisations": [
          "signals",
          "counter_signals",
          "evidence",
          "human_needs",
          "territories"
        ],
        "consumers": [
          "signals",
          "counter_signals",
          "evidence"
        ]
      }
    }
  },
  "types": {
    "dek": {
      "summary": "Standfirst under the shift hero.",
      "data": {
        "text": "string"
      },
      "required": [
        "text"
      ]
    },
    "lede": {
      "summary": "Opening line of a sub-shift page.",
      "data": {
        "text": "string"
      },
      "required": [
        "text"
      ]
    },
    "from_to": {
      "summary": "The shift's spine as two cards whose gradient fills cross-fade.",
      "data": {
        "from": "string",
        "to": "string"
      },
      "required": [
        "from",
        "to"
      ]
    },
    "from_to_solid": {
      "summary": "From/To pair with solid green/pink fills (sub-shift variant).",
      "data": {
        "from": "string",
        "to": "string"
      },
      "required": [
        "from",
        "to"
      ]
    },
    "stat_band": {
      "summary": "Full-bleed band: oversized numeral, prose, source.",
      "data": {
        "value": "string",
        "text": "string",
        "source": "string",
        "url": "string?"
      },
      "required": [
        "value"
      ]
    },
    "peel_tabs": {
      "summary": "Two stacked cards the reader toggles: what's changing / why now.",
      "data": {
        "whats_changing": "string",
        "why_now": "string",
        "evidence_ids": [
          "integer"
        ]
      },
      "required": [
        "whats_changing",
        "why_now",
        "evidence_ids"
      ]
    },
    "sub_shift_list": {
      "summary": "Cards linking to this shift's sub-shifts. Resolved from context, not data.",
      "data": {},
      "required": []
    },
    "human_needs": {
      "summary": "A card pair set side by side: the need unlocked and the need threatened. Both halves are required \u2014 the design shows them at once rather than behind a toggle, so a missing side is a coloured rectangle with a label and no copy.",
      "data": {
        "unlocked": "string",
        "threatened": "string"
      },
      "required": [
        "unlocked",
        "threatened"
      ]
    },
    "tension_band": {
      "summary": "Dark full-bleed band carrying the consumer tension, in the consumer's own voice.",
      "data": {
        "quote": "string",
        "label": "string?"
      },
      "required": [
        "quote"
      ]
    },
    "pull_quote": {
      "summary": "The editorial line that lands the shift, set immediately after From/To. Distinct from tension_band: this is the analyst's verdict, not the consumer's voice.",
      "data": {
        "quote": "string"
      },
      "required": [
        "quote"
      ]
    },
    "innovations": {
      "summary": "\"Innovations in the wild\" \u2014 real branded examples of the shift, from the innovations table. Not generated: the backend hydrates this module's items from innovation_shift_links when it builds a route fragment, so a newly-ingested example appears without a publication run. `image` is always a same-origin /api/innovations/{id}/cover-image path, because the CSP is img-src 'self'.",
      "data": {
        "items": [
          {
            "title": "string",
            "brand": "string?",
            "description": "string?",
            "url": "string?",
            "image": "string?",
            "tags": [
              "string"
            ]
          }
        ]
      },
      "required": [
        "items"
      ]
    },
    "timeline": {
      "summary": "Now / next / beyond, as a vertical timeline.",
      "data": {
        "steps": [
          {
            "label": "string",
            "text": "string"
          }
        ]
      },
      "required": [
        "steps"
      ]
    },
    "industries": {
      "summary": "Sector chips plus a detail card that swaps on selection.",
      "data": {
        "items": [
          {
            "name": "string",
            "text": "string"
          }
        ]
      },
      "required": [
        "items"
      ]
    },
    "territories": {
      "summary": "Opportunity-territory cards plus a work-with-us card.",
      "data": {
        "items": [
          {
            "name": "string",
            "text": "string"
          }
        ]
      },
      "required": [
        "items"
      ]
    },
    "signals": {
      "summary": "Numbered evidence that the shift is real (sub-shift).",
      "data": {
        "items": [
          "string"
        ]
      },
      "required": [
        "items"
      ]
    },
    "counter_signals": {
      "summary": "The honest case against (sub-shift).",
      "data": {
        "items": [
          "string"
        ]
      },
      "required": [
        "items"
      ]
    },
    "voices": {
      "summary": "Who backs this shift and who disputes it, with their own words. Built at export from the thinker-attribution phase \u2014 no extra model spend.",
      "data": {
        "proponents": [
          {
            "name": "string",
            "quote": "string",
            "source": "string?",
            "date": "string?",
            "url": "string?"
          }
        ],
        "skeptics": [
          {
            "name": "string",
            "quote": "string",
            "source": "string?",
            "date": "string?",
            "url": "string?"
          }
        ]
      },
      "required": []
    },
    "evidence": {
      "summary": "The sourced claims behind a sub-shift: what was said, by whom, where, and when. Distinct from `signals`, which is written prose.",
      "data": {
        "items": [
          {
            "text": "string",
            "thinker": "string",
            "source": "string?",
            "date": "string?",
            "url": "string",
            "strength": "string?",
            "implication": "string?"
          }
        ]
      },
      "required": [
        "items"
      ]
    },
    "related_shifts": {
      "summary": "Typed links to other shifts (reinforces, accelerated_by, \u2026) from the interrelatedness phase.",
      "data": {
        "items": [
          {
            "title": "string",
            "href": "string",
            "relationship": "string",
            "reasoning": "string?",
            "domain": "string?"
          }
        ]
      },
      "required": [
        "items"
      ]
    },
    "rich_text": {
      "summary": "Escape hatch for editor-authored prose. Never generated.",
      "data": {
        "heading": "string?",
        "body": "string"
      },
      "required": [
        "body"
      ]
    }
  },
  "industry_sectors": [
    "Beauty & Personal Care",
    "Consumer Tech",
    "Digital Tech",
    "Entertainment",
    "Fashion & Accessories",
    "Financial Services",
    "Food & Beverage",
    "Government & Public Sector",
    "Health & Wellbeing",
    "Home & Living",
    "Media & Publishing",
    "Mobility & Transport",
    "Nonprofit & Social Cause",
    "Retail & Commerce",
    "Travel & Hospitality",
    "Work & Education"
  ],
  "$sectors_comment": "The 16 TrendWatching sectors. The industries module asks for one entry per sector, in this order; kt_editorial.txt must match this list."
}
