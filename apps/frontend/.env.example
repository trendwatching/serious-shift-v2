# Dev-server only: where `npm run dev` proxies /api/* to.
# In production the backend serves this bundle from its own origin, so nothing
# here is needed — and nothing is inlined into the client bundle.
BACKEND_ORIGIN=http://localhost:8080
