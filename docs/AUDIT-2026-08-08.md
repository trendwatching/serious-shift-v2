# System audit — 2026-08-08

Measured against the live deployments, today's two real pipeline runs
(`20260808T081415-ingest-aeb031`, `20260808T083603-synthesize-01a9e2`), the
staging database, and GitHub Actions history. Nothing here is read off the
source and assumed.

Severity is consequence, not effort:

- **S1** wrong in production or in the delivery path, or money spent for nothing
- **S2** correct today, and nothing signals when it stops
- **S3** waste, inconsistency, or a latent trap

**33 findings. 9 are S1.** The three that matter most are at the top.

---

## 0. The three that matter most

### E1 — Production is running the old site (S1)

`www.seriousshift.ai` serves the **pre-rebuild Next.js app**: `/_next/static/…`
bundles, title *"Serious Shi(f)t — AGI Consumer Intelligence"*. None of this
work is public.

The production backend is worse — it is `Online` and answers **404 on every
route**, including `/`, `/robots.txt` and `/api/v1/map`. It has no map document
and serves no SPA. The site people can actually visit is a separate `frontend`
Railway service holding the old build, which exists in `dev` and `production`
with **no config file** and is invisible to `railway.*.json`.

Staging is the only working environment. The cutover is entirely pending and
`docs/PRODUCTION-CUTOVER.md` has not been executed.

### C1 — CI has been red all day, on every push (S1)

Four pushes today, four `frontend` workflow failures. I verified everything
locally each time and never opened Actions. Three tests fail:

- **Two homepage visual snapshots** (390×844, 393×852). The baselines are
  generated on macOS and CI runs Ubuntu, so font rasterisation differs by more
  than `maxDiffPixelRatio: 0.015`. This can never pass — the test is
  structurally broken, not flaky.
- **`a route change lands at the top, and a nav anchor lands on its section`** —
  a real bug, below.

`governance` passes. `backend`, `db` and `pipeline` are path-filtered and did not
run on the frontend-only pushes.

### F1 — Five of the six nav rows land in the wrong place (S1)

Clicking **Services** in the nav lands **330–570px above** `#services`, in the
middle of the previous section — and the distance differs run to run (CI
measured 333.9 and 571.9 on consecutive runs).

`ScrollManager` scrolls in `useLayoutEffect`, which fires before any image has
loaded. The About page's section images are `<img class="h-auto w-full">` with
**no `width`, `height` or `aspect-ratio`**, so they occupy zero height at that
moment and then shove the target down as they arrive. Five of six nav rows are
`/about#section`, so five of six are affected. It passes on a fast local machine
by luck, which is exactly why only CI caught it.

---

## 1. Delivery and environments

| # | Sev | Finding |
|---|-----|---------|
| **E1** | S1 | Production serves the old Next.js build; production backend 404s on every route. See above. |
| **E2** | S2 | An orphaned `frontend` service exists in `dev` and `production` with `railwayConfigFile = null`. It is what serves the live site, and nothing in the repo describes it. |
| **E3** | S2 | Railway Postgres backups are a dashboard setting, not in the repo. Nothing in version control asserts a backup exists. 57MB of claims accumulated over two weeks has no stated recovery path. |
| **E4** | S3 | `railway.backend.json` sets neither `cronSchedule` nor `healthcheckPath`; the two pipeline services define their crons in config-as-code but the backend's deploy shape is implicit. |

## 2. CI and test integrity

| # | Sev | Finding |
|---|-----|---------|
| **C1** | S1 | Frontend workflow red on every push today. See above. |
| **C2** | S1 | **Visual snapshots cannot pass in CI.** macOS baselines, Ubuntu runner. Either generate them in the same container CI uses, or drop the assertion — a permanently-red test trains everyone to ignore the pipe. |
| **C3** | S2 | **No Rust dependency scanning.** `npm audit --omit=dev` runs and is clean; `cargo audit` is not installed and not in CI. |
| **C4** | S3 | 17 pipeline tests skip locally on `DATABASE_URL not set` — all of `test_watermark.py`. CI sets it, so they do run there, but a developer running `pytest` sees green without them. |
| **C5** | S3 | Nothing pins `packages/contracts/shift_modules.json`'s `version: 5`. No consumer asserts it, so a breaking contract change cannot fail a build — only the registry-drift test would catch a *shape* change. |

## 3. Ingestion

One run: 353 raw files, 1,356 claims, 21 minutes, $1.45, **173 recorded errors**.

| # | Sev | Finding |
|---|-----|---------|
| **I1** | S2 | **The scraper follows non-article URLs.** `SKIP_PATTERNS` filters path substrings and has no extension rule, so `.pdf`, `.jpg`, `.zip` are queued as articles and fail. 23 of 115 fetch failures are assets, including `BobTagxedoSmall.jpg`. |
| **I2** | S2 | **Three sources produce 74 of the 115 failures**: Charles I. Jones (30), OpenAI (24), Jaron Lanier (20). Nothing distinguishes "misconfigured" from "quiet week". |
| **I3** | S3 | **6 YouTube sources are IP-blocked** — Reid Hoffman, Yuval Noah Harari, Scott Galloway and three more contribute nothing. Needs `YOUTUBE_PROXY_URL` or `WEBSHARE_PROXY_*`; neither is set. |
| **I4** | S3 | **70 files discarded as `file_too_small`** (<1500 bytes) with no aggregate reporting. Genuinely-short posts and nav shells are indistinguishable. |
| **I5** | S3 | **PDFs are dropped rather than read.** For the academic sources they *are* the content. |
| **I6** | S3 | The run summary prints `Errors: 72` while **173 rows** were written to `pipeline_errors` for that run. The console figure and the record disagree. |

*Not a bug:* 157/353 "processed" is correct — 124 were already in the database and
skipped before paid extraction.

## 4. Synthesis

| # | Sev | Finding |
|---|-----|---------|
| **S1** | S1 | **Phase 5 is paid for and never seen.** 48 API calls a run produce `proponents`/`skeptics`; the fragment strips both and the `voices` module is hidden on all four spheres. `proponents` is `None` on all 51 published shifts. |
| **S2** | S1 | **Phase 7 is paid for and never rendered.** `insights` reaches `useDomains`; no component consumes it. |
| **S3** | S2 | **`velocity` carries no signal** — `accelerating` on 51 of 51. Projected into the view-model, never rendered. |
| **S4** | S2 | A failed publication discards ~25 minutes of generation. The gate runs once, at the end; recovery is a free `--export-only`, and the failure message does not say so. |
| **S5** | S3 | `shift_refs` never shrinks: **636 rows for 306 published shifts**, 330 stale. |
| **S6** | S3 | `--export-only` publishes without opening a `pipeline_runs` row, so the history reads "last synthesize: failed" while the map is fresh. |

## 5. Innovations

| # | Sev | Finding |
|---|-----|---------|
| **N1** | S1 | **The pipe is built and nothing feeds it.** 5 innovations, newest 2026-08-05. `POST /api/innovations/ingest` is correct — authenticated, idempotent, rate-limited, 401 on a bad token — but it is a push API owned by the upstream database, and that system is not pushing. **1 of 51 key shifts** renders the module. |
| **N2** | S3 | Route family split: reads are `/api/v1/innovations*`, writes and images `/api/innovations/*`. Every other public surface is `/api/v1`. |
| **N3** | S3 | The classifier's escalation path had never executed before today — four stacked bugs on the branch that only fires when the lexical scorer is undecided. With 5 innovations it remains barely exercised. |

## 6. Frontend

| # | Sev | Finding |
|---|-----|---------|
| **F1** | S1 | Nav anchors land 330–570px off. See above. |
| **F2** | S2 | **20 of 22 images ship with no `width`/`height`.** Home measures CLS 0.083 — under the 0.1 threshold, but the same missing dimensions are what break F1. |
| **F3** | S3 | CSP allows `script-src 'unsafe-inline'` and the shell ships **zero inline scripts**. The allowance is unnecessary and weakens the policy for nothing. |
| **F4** | S3 | `/about` ships **797KB**, dominated by unoptimised JPEGs (`about-whatsapp.jpg` is 184KB decorative). Every other page is 143KB. |
| **F5** | S2 | Hero art is slug-keyed and built at deploy time — any republish that renames shifts silently drops all 306 images, falling back to the sphere gradient, which looks deliberate. |
| **F6** | S3 | Six dead exports: `useNavigate`, `SERVICES_URL`, `LINKEDIN_URL`, `FOOTER_LINKS`, `SOCIALS`, `quoted`. |
| **F7** | S3 | Pointer-dependent copy on desktop: "Tap to open", "SWIPE FOR SOCIETY". |

## 7. Backend

| # | Sev | Finding |
|---|-----|---------|
| **K1** | S3 | One `lock().unwrap()` on the rate-limiter mutex. A panic while holding it poisons it and every subsequent request panics. |
| — | ok | **All SQL is static or bound.** No dynamic query construction anywhere. |
| — | ok | **Latency 29–33ms median** across the shell and all three map routes; conditional GET returns 304; brotli negotiated. |
| — | ok | Security headers are strong: HSTS, `X-Frame-Options: DENY`, `nosniff`, `frame-ancestors 'none'`, `object-src 'none'`, strict referrer policy. |
| — | ok | Ingest and curation endpoints both return 401 without a valid token; ingest 404s when the token is unset. |

## 8. Data

| # | Sev | Finding |
|---|-----|---------|
| **D1** | S2 | **No retention on the big tables.** `pipeline_errors` and `pipeline_runs` are pruned; `claims` (45,355 rows / 57MB), `sources` (2,512 / 21MB) and `predictions` (9,491) are not. Two weeks of history, growing ~3k claims a week. |
| **D2** | S3 | 330 stale `shift_refs` (see S5). |
| — | ok | Referential integrity is clean: no orphaned claims, no dangling sub-shift claim links, no innovation links pointing at missing refs. |

---

## Plan

Ordered by consequence. Each step is independently shippable.

### Step A — Unbreak the delivery path (~half a day)

1. **C2.** Make the visual snapshots run in the same Linux container CI uses
   (`--docker` or a `mcr.microsoft.com/playwright` job), or delete the
   assertion. A test that cannot pass is worse than no test.
2. **F1 + F2.** Give every `<img>` intrinsic dimensions or an `aspect-ratio` so
   space is reserved before load, and have `ScrollManager` re-scroll once the
   images above the target have settled. Fixes the anchor bug deterministically
   and takes CLS to zero.
3. Re-run CI and get it green. **Nothing else should be merged until it is.**

### Step B — Decide production (~1 day, mostly decisions)

4. **E1.** Execute `docs/PRODUCTION-CUTOVER.md`: point the production backend at
   the new build, publish a map into the production database, and retire the
   `frontend` service — or consciously decide staging stays the demo surface.
5. **E3.** Confirm and document the Postgres backup policy.

### Step C — Stop paying for what nobody sees (~2 hours)

6. **S1, S2, S3.** Voices, insights, velocity: turn on or delete. ~52 API calls a
   run either way. My recommendation is still to unhide `voices` — the content
   exists, is good, and is the only place the thinker corpus surfaces to a reader.

### Step D — Make failure legible (~3 hours)

7. **I2 + I4 + I6.** Per-source health in the run summary (found / fetched /
   too-small / failed / claims), and a non-zero signal when a source yields
   nothing twice running. This converts I1–I5 from "someone notices eventually".
8. **I1.** Extension filter in `SKIP_PATTERNS` — removes 23 failures a run.
9. **S4 + S6.** Name `--export-only` in the failure message; open a run row for it.

### Step E — Durability (~half a day)

10. **F5.** Make `check-heroes` compare the manifest against the **published
    slugs**, not just the directory. Five lines; turns a silent, invisible
    regression into a red build.
11. **S5.** Reconcile `shift_refs` at publication and re-run `classify` inside the
    synthesize stage so renames self-heal.
12. **D1.** A retention window on `claims`/`sources`/`predictions`, or an explicit
    decision that the archive is the point.
13. **C3.** Add `cargo audit` to CI. **F3.** Drop `script-src 'unsafe-inline'`.
    **F4.** Re-encode the About images. **F6.** Delete the dead exports.

### Not doing, and why

- **I3 (YouTube proxy)** needs a paid credential — a decision, not a fix.
- **N1 (innovations feed)** needs the upstream owner, not a patch. Everything on
  this side is proven: today's classifier run linked one innovation end to end.
- **F7 (pointer copy)** — the design says "Tap"; varying it by input type is a
  content decision.
- **K1** — a poisoned mutex requires a prior panic; the panic is the bug to fix if
  one ever appears.
