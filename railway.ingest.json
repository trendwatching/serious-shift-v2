{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "apps/pipeline/Dockerfile" },
  "deploy": {
    "startCommand": "python -m serious_shift_pipeline.run ingest",
    "cronSchedule": "0 22 * * 0",
    "restartPolicyType": "NEVER"
  }
}
